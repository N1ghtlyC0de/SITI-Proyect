services:
  - type: web
    name: siti-backend
    env: python
    buildCommand: pip install -r backend-fastapi/requirements.txt
    startCommand: uvicorn backend-fastapi.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
